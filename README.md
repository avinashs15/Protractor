# Protractor
This sample piece of framework is wrotten using Protractor and some npm modules.
User needs to install all the required modules before executoion.
the conf.js is the starting point which controles the execution of the test cases

more info needs to be included
